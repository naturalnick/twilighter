{
	"name": "twilighter",
	"version": "1.0.0",
	"main": "",
	"scripts": {
		"start": "npm start --prefix server",
		"install-client": "cd client && npm install && npm run build && cd ..",
		"install-server": "cd server && heroku buildpacks:add --index 1 heroku/python && pip install -r requirements.txt && gunicorn server:app",
		"heroku-prebuild": "npm install",
		"heroku-postbuild": "npm run install-client && npm run install-server"
	}
}
